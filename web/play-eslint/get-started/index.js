/* eslint-disable no-constant-condition, no-cond-assign, linebreak-style, no-console */

const user = {
    jobTitle: 'programmer',
    saySomething: () => {
        console.log('hi');
    }
};

if (user.jotTitle = 'engineer') {
    // dosometing
}

var saySomething = ::user.saySomething;